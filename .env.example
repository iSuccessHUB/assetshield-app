# AssetShield Admin Configuration
# CRITICAL: Change these values in production

# Admin Authentication
ADMIN_USERNAME=admin@isuccesshub.com
ADMIN_PASSWORD=AdminPass2024!Change
ADMIN_JWT_SECRET=your-super-secure-jwt-secret-here

# Optional: Pre-configured 2FA secret (if you want to set it manually)
ADMIN_TOTP_SECRET=

# Stripe Configuration (Production)
STRIPE_SECRET_KEY=sk_live_...
STRIPE_PUBLISHABLE_KEY=pk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Database
DATABASE_URL=your-production-database-url

# Security Settings
NODE_ENV=production
RATE_LIMIT_WINDOW=900000
RATE_LIMIT_MAX_REQUESTS=100

# IMPORTANT SECURITY NOTES:
# 1. Never commit this file to git with real values
# 2. Always use strong passwords (20+ characters, mixed case, numbers, symbols)
# 3. Generate unique JWT secrets using: openssl rand -base64 32
# 4. Enable 2FA immediately after first login
# 5. Monitor admin access logs regularly
# 6. Consider IP whitelisting for admin access